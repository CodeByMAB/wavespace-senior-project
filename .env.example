# Copy to `.env` and fill in. After changing this key, run `npm run start:dev` (clears Metro cache)
# or `npx expo start --dev-client --clear` so the bundle picks up the new value.
# Get a key from https://breez.technology (Spark / SDK).
EXPO_PUBLIC_BREEZ_API_KEY=""
